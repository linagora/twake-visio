pre-commit:
  commands:
    format:
      run: npm run format:check
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck
commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
